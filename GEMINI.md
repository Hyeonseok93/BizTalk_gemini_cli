# GEMINI 프로젝트 컨텍스트: 페르소나 스위처 (BizTalk_gemini_cli)

이 프로젝트는 Groq API를 활용하여 일상적인 문장을 특정 페르소나(예: 조선 선비, 마피아, AI 리포터 등)의 말투로 변환해주는 몰입형 AI 텍스트 변환 도구입니다. 선택한 페르소나에 따라 디자인, 폰트, 애니메이션이 실시간으로 변하는 "카멜레온 UI(Chameleon UI)"가 특징입니다.

## 🏗 프로젝트 개요
- **목적**: 페르소나별 텍스트 변환과 몰입형 UI/UX를 통해 사용자가 해당 캐릭터에 '빙의'하는 듯한 경험 제공.
- **핵심 기술**:
    - **AI 엔진**: Groq API (Llama 3/4 모델 활용)를 통한 초고속 텍스트 변환.
    - **백엔드**: Python Flask REST API.
    - **프론트엔드**: Tailwind CSS 기반의 Vanilla JavaScript, 고성능 테마 전환을 위한 커스텀 CSS 및 Canvas 애니메이션.
    - **카멜레온 UI**: CSS 변수, 폰트, 배경 효과를 실시간으로 교체하는 동적 테마 엔진.

## 📂 프로젝트 구조
- `server/`: Flask 백엔드.
    - `app.py`: 메인 엔트리 포인트, API 엔드포인트(`/api/v1/personas`, `/api/v1/transform`) 처리.
    - `persona_manager.py`: 페르소나 메타데이터, 시스템 지침(System Instruction), 사용자 템플릿 관리.
    - `requirements.txt`: Python 의존성 패키지 목록.
- `client/`: 프론트엔드 자산.
    - `index.html`: 메인 애플리케이션 인터페이스 (Tailwind CSS 적용).
    - `css/`: `themes.css` (페르소나별 CSS 변수), `animations.css` (글리치 및 뒤집기 효과).
    - `js/`: `api-client.js` (API 통신), `theme-engine.js` (테마 전환 로직).
- `docs/`: `PRD.md` 및 `프로그램개요서.md`를 포함한 프로젝트 문서.

## 🚀 빌드 및 실행 방법
### 백엔드 (Flask)
1. 프로젝트 루트 디렉토리로 이동합니다.
2. `server/` 디렉토리에 `GROQ_API_KEY`가 포함된 `.env` 파일이 있는지 확인합니다.
3. 의존성 패키지를 설치합니다:
   ```bash
   pip install -r server/requirements.txt
   ```
4. 서버를 실행합니다:
   ```bash
   python server/app.py
   ```
   *서버는 기본적으로 `http://localhost:5000`에서 실행됩니다.*

### 프론트엔드
- 브라우저에서 `client/index.html`을 열거나, 정적 파일 서빙이 설정된 Flask 서버를 통해 접속합니다.

## 🛠 개발 컨벤션
- **페르소나 데이터**: 모든 페르소나 정의(이름, 색상, 폰트, 시스템 프롬프트)는 `server/persona_manager.py`에서 관리됩니다. 여기에 새로운 항목을 추가하면 UI에 자동으로 반영됩니다.
- **테마 전환**: `client/js/theme-engine.js`의 `ThemeEngine` 클래스가 `<body>` 태그의 `data-theme` 속성을 업데이트하며, 이는 `themes.css`에 정의된 CSS 변수 변경을 트리거합니다.
- **UI/UX**: "글래스모피즘(Glassmorphism)" 미학과 부드러운 3D 트랜지션(예: 마우스 호버 시 가면 180도 회전)을 우선시합니다.
- **AI 프롬프팅**: 시스템 지침은 불필요한 부연 설명 없이 변환된 텍스트만 반환하도록 엄격하게 설정(Zero-Shot)해야 합니다.

## ✅ 향후 작업 과제 (TODO)
- [ ] 소셜 공유용 카드(PNG) 생성 기능 구현.
- [ ] 캐릭터별 특화 Canvas 효과 추가 (예: 마피아 테마의 빗줄기, 선비 테마의 먹번짐 효과).
- [ ] 모바일 자이로 센서를 활용한 반응형 배경 움직임 최적화.
